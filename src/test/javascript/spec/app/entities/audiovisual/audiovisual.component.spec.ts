import { ComponentFixture, TestBed } from '@angular/core/testing';
import { of } from 'rxjs';
import { HttpHeaders, HttpResponse } from '@angular/common/http';
import { ActivatedRoute, Data } from '@angular/router';

import { ToDoTaskManagerTestModule } from '../../../test.module';
import { AudiovisualComponent } from 'app/entities/audiovisual/audiovisual.component';
import { AudiovisualService } from 'app/entities/audiovisual/audiovisual.service';
import { Audiovisual } from 'app/shared/model/audiovisual.model';

describe('Component Tests', () => {
  describe('Audiovisual Management Component', () => {
    let comp: AudiovisualComponent;
    let fixture: ComponentFixture<AudiovisualComponent>;
    let service: AudiovisualService;

    beforeEach(() => {
      TestBed.configureTestingModule({
        imports: [ToDoTaskManagerTestModule],
        declarations: [AudiovisualComponent],
        providers: [
          {
            provide: ActivatedRoute,
            useValue: {
              data: {
                subscribe: (fn: (value: Data) => void) =>
                  fn({
                    pagingParams: {
                      predicate: 'id',
                      reverse: false,
                      page: 0
                    }
                  })
              }
            }
          }
        ]
      })
        .overrideTemplate(AudiovisualComponent, '')
        .compileComponents();

      fixture = TestBed.createComponent(AudiovisualComponent);
      comp = fixture.componentInstance;
      service = fixture.debugElement.injector.get(AudiovisualService);
    });

    it('Should call load all on init', () => {
      // GIVEN
      const headers = new HttpHeaders().append('link', 'link;link');
      spyOn(service, 'query').and.returnValue(
        of(
          new HttpResponse({
            body: [new Audiovisual(123)],
            headers
          })
        )
      );

      // WHEN
      comp.ngOnInit();

      // THEN
      expect(service.query).toHaveBeenCalled();
      expect(comp.audiovisuals && comp.audiovisuals[0]).toEqual(jasmine.objectContaining({ id: 123 }));
    });

    it('should load a page', () => {
      // GIVEN
      const headers = new HttpHeaders().append('link', 'link;link');
      spyOn(service, 'query').and.returnValue(
        of(
          new HttpResponse({
            body: [new Audiovisual(123)],
            headers
          })
        )
      );

      // WHEN
      comp.loadPage(1);

      // THEN
      expect(service.query).toHaveBeenCalled();
      expect(comp.audiovisuals && comp.audiovisuals[0]).toEqual(jasmine.objectContaining({ id: 123 }));
    });

    it('should calculate the sort attribute for an id', () => {
      // WHEN
      comp.ngOnInit();
      const result = comp.sort();

      // THEN
      expect(result).toEqual(['id,desc']);
    });

    it('should calculate the sort attribute for a non-id attribute', () => {
      // INIT
      comp.ngOnInit();

      // GIVEN
      comp.predicate = 'name';

      // WHEN
      const result = comp.sort();

      // THEN
      expect(result).toEqual(['name,desc', 'id']);
    });
  });
});
